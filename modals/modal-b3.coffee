b3 = @b3

#rendering
Template.videoModal.rendered = ->
    console.log 'videoModal rendered', @
Template.dashboardModal.rendered = ->
    console.log 'dashboardModal rendered', @

Template.canvasModal.rendered = ->
    console.log 'canvasModal rendered', @

Template.standardModal.rendered = ->
    console.log 'standardModal rendered', @

Template.choiceModal.rendered = ->
    console.log 'choiceModal rendered', @

Template.answerModal.rendered = ->
    console.log 'aswerModal rendered', @
